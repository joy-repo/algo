package task;

import java.nio.file.Files;
import java.util.List;
import java.util.stream.Collectors;

public class Deserializer {

  public static void main(String[] args) {
   // List<LogDTO> ldto = Files.lines("<<path>>").map(s -> <<Converter to convert it to LogDTO). collect(Collectors.toList())

  //  ldto.stream().filter(l-> l.getstatus().e).sorted()
  }


}
